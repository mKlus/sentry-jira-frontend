{
    "name": "sentry-jira-frontend",
    "source": "src/index.html",
    "browserslist": "> 0.5%, last 2 versions, not dead",
    "scripts": {
        "start": "parcel",
        "build": "parcel build"
    },
    "devDependencies": {
        "parcel": "latest",
        "process": "^0.11.10"
    },
    "dependencies": {
        "@sentry/browser": "^6.18.2",
        "@sentry/tracing": "^6.18.2"
    }
}
